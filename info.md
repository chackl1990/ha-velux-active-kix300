## Velux Active KIX 300

HACS-compatible Home Assistant custom integration for VELUX ACTIVE (KIX 300) using the cloud API.

**Not official** – uses an unofficial/undocumented endpoint.  
Intended only to fetch **missing diagnostics/metadata**. Use **HomeKit** for open/close control and CO₂ values.
